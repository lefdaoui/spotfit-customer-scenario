export enum GymOrderBy {
  DISTANCE,
  REVIEW,
  CHEAP
}


export enum SubscriptionStatus {
  CANCELED,
  ACTIVE,
  CONFIRMED,
  EXPIRED,
  RESERVED,
}

export enum SubscriptionType {
  STRICT,
  EVERYWHERE,
  DIVERS,
}


export enum NotificationType {
  MESSAGE,
  SUBSCRIPTION,
  RELATIONSHIPS,
}
